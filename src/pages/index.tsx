import { Typography } from '@mui/material';
import { AppLayout } from '@/layouts';

function Home() {
  return (
    <AppLayout>
      <Typography variant="h3">NextJs App Starter Template</Typography>
    </AppLayout>
  );
}

export default Home;
